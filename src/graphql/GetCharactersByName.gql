query {
  characters(page: 1, filter: { name: "rick" }) {
    info {
      count
    }
    results {
      id,
      name,
      image,
      species
    }
  }
  location(id: 1) {
    id
  }
  episodesByIds(ids: [1, 2]) {
    id
  }
}
